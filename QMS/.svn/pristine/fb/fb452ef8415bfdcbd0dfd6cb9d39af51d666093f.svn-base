import Vuex from 'vuex'
import Vue from 'vue'

const store = {
    state: {
        eable:null,
        data:null
    },
    getters: {
        bookingTemplateCommitState:(state)=>state.eable,
        bookingTemplateFormData:(state)=>state.data
    },
    mutations:{
        setBookingTemplateCommitState(state,_eable){
            state.eable=_eable;
        },
        setBookingTemplateFormData(state,_data){
            state.data=_data;
        }
    },
    action:{

    }

};
export default store;